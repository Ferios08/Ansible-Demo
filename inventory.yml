web:
  hosts:
    web1:
      ansible_connection: ssh # can be local in case host is localhost
      ansible_host: 
    web2:
      ansible_connection: ssh # can be local in case host is localhost
      ansible_host: 

  vars:
    domain: helloworld.com
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
    ansible_sudo_pass: qwerty

db:
  hosts:
    web1:
      ansible_connection: ssh # can be local in case host is localhost
      ansible_host: 
  
  vars:
    mysql_root_password: helloworld.com
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
    ansible_sudo_pass: qwerty